<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>result</title>
  </head>
  <body>
    <h1>pooh님 마이페이지</h1>
    <img src="<%=img.path%>" alt="" />
    <!-- index에서 정보를 전송한다음에 변수값으로 받아오면 될듯. 이미지는 찍어보고 url
     값을 넣어주면 될듯..? -->
    <p>아이디 : <%=userInfo.email%></p>
    <p>아이디 : <%=userInfo.password%></p>
    <p>아이디 : <%=userInfo.name%></p>
    <p>아이디 : <%=userInfo.age%></p>
  </body>
</html>
